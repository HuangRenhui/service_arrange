# 测试指南

本文给出服务编排平台的测试策略、验证清单、接口自测方法与缺陷复现步骤。

> 说明：仓库当前**没有任何自动化测试用例**（无 `src/test` 目录）。本文提供的是**手工验证清单**与**后续补测建议**。

---

## 📋 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 测试指南 |
| 文档版本 | v1.0 |
| 最后更新 | 2026-09-14 |
| 状态 | 现行 |
| 自动化测试 | ❌ 无（待补齐）|

---

## 目录

- [§1 测试策略](#§1-测试策略)
- [§2 测试环境](#§2-测试环境)
- [§3 解析层验证](#§3-解析层验证)
- [§4 接口验证](#§4-接口验证)
- [§5 DSL 样例验证](#§5-dsl-样例验证)
- [§6 缺陷复现与验证](#§6-缺陷复现与验证)
- [§7 测试清单](#§7-测试清单)

---

## §1 测试策略

### 1.1 分层测试金字塔（目标态）

```
        ┌─────────────────┐
        │  端到端（E2E）   │  流程编排 → 运行 → 校验出参
        └─────────────────┘
      ┌───────────────────────┐
      │   集成测试             │  DslParser / 调度链路 / 接口
      └───────────────────────┘
    ┌─────────────────────────────┐
    │        单元测试              │  表达式求值 / Schema 工具 / 校验规则
    └─────────────────────────────┘
```

### 1.2 当前可测范围

| 层次 | 可测性 | 说明 |
| --- | --- | --- |
| 前端校验规则 | ✅ 易测 | `DslValidator.validate()` 是纯函数，可直接单测 |
| 表达式求值 | ✅ 可测 | `TaskServiceImpl#edgeDecisionDo` 逻辑独立，但需 mock 实例数据 |
| DSL 解析 | ✅ 可测 | `DslParser#parser` 输入字符串输出 `Inst`，是最佳切入点 |
| Schema 工具 | ✅ 可测 | `JsonSchemaUtil` 纯函数 |
| 调度链路 | ⚠️ 难测 | 依赖 Mongo + MQ + ZK，需集成环境 |
| 算子执行 | ⚠️ 部分可测 | `DataMapOperateExecutor` 逻辑可测；HTTP 算子需 mock |
| 端到端 | ❌ 不可测 | 因 24 种类型仅 2 个转换器，主链路跑不通（见 [项目现状](10-项目现状.md)）|

### 1.3 补测优先级建议

| 优先级 | 测试对象 | 理由 |
| --- | --- | --- |
| 1 | `DslParser` | 引擎入口，图构建正确性决定一切；纯函数易测 |
| 2 | `DslValidator`（前端） | 纯函数，覆盖 R1~R9 全规则 |
| 3 | `TaskServiceImpl` 表达式求值 | 决策/循环的正确性核心 |
| 4 | `JsonSchemaUtil` | 入参类型还原依赖它，边界多 |
| 5 | `DataMapOperateExecutor` | 取值逻辑分支多，易出边界 bug |
| 6 | 调度链路集成测试 | 需引入 testcontainers 或嵌入式 Mongo |

---

## §2 测试环境

### 2.1 环境要求

| 组件 | 用途 |
| --- | --- |
| MongoDB | 测试库，建议独立于开发库 |
| RabbitMQ | 消息通道 |
| ZooKeeper | 实例锁 |

### 2.2 隔离建议

| 项 | 建议 |
| --- | --- |
| 数据库 | 使用独立库名，如 `servicearrange_test` |
| 队列 | 使用独立 `group`，如 `/task/run_test` |
| ZK 路径 | 建议加环境前缀（需代码支持）|
| 数据清理 | 每次测试前清空四个集合 |

```javascript
// 测试前清理
use servicearrange_test
db.SERVEA_INST.deleteMany({})
db.SERVEA_TASK.deleteMany({})
db.SERVEA_INST_LOG.deleteMany({})
db.REGISTER_INFO.deleteMany({})
```

---

## §3 解析层验证

`DslParser` 是最值得优先补测的组件。以下用例可直接转成 JUnit。

### 3.1 基础图构建

| 用例 | 输入 | 期望 |
| --- | --- | --- |
| 最简流程 | 开始 → 结束 | `roots = [startId]`；`nodeChildsMap[startId] = {endId}` |
| 线性三节点 | 开始 → A → 结束 | A 的 parent = `{startId}`；end 的 parent = `{A}` |
| 多分支 | 开始 → A、开始 → B | 两者 parent 均为 `{startId}` |
| 汇聚 | A → C、B → C | C 的 parents = `{A, B}` |
| 孤立节点 | 一个不与任何线相连的节点 | 同时进入 `roots` 与 `ends` |

### 3.2 真实入口/出口推导（对齐 `DslParser` L79/L82）

| 用例 | 连线 | 期望 `roots`（真实入口） | 期望 `ends`（真实出口） |
| --- | --- | --- | --- |
| 线 1-2、2-3 | src={1,2}，tar={2,3} | `{1}` | `{3}` |
| 线 1-2、1-3 | src={1}，tar={2,3} | `{1}` | `{2,3}` |
| 线 1-3、2-3 | src={1,2}，tar={3} | `{1,2}` | `{3}` |

> 验证要点：`roots` 最终**只含 `startId`**（虚拟开始节点挂接后），真实入口成为 `startId` 的 children。

### 3.3 循环区间推导

| 用例 | 结构 | 期望 `NodeLoopInfo.idsBetweenLoopEdge` |
| --- | --- | --- |
| 简单循环 | 循环线 A→B，中间节点 C | 含 A、B、C 及两端的内部连线 |
| 嵌套循环 | 外循环 A→D 内含内循环 B→C | 外循环区间含全部；内循环区间只含 B、C |

> 验证要点：区间 = 「source 的所有祖先」∩「target 的所有后代」+ 两端点。

### 3.4 强组合填充

| 用例 | 输入 | 期望 |
| --- | --- | --- |
| 单节点入组 | `cells[0].groupIds = ["g1"]`，`groups = [{id:"g1", nodes:[]}]` | `groups[0].nodes = [cell.id]` |
| 多节点入组 | 两个节点 `groupIds` 均为 `["g1"]` | `groups[0].nodes` 含两个 id |

### 3.5 全局参数解析

| 用例 | 输入 | 期望 |
| --- | --- | --- |
| 静态参数 | `staticParams: [{name:"a", type:"string", default:1}]` | `Inst.staticParams[0].key = "a"` |
| 动态参数 | `dynamicParams: [{name:"idx", type:"number", default:0}]` | `Inst.dynamicParams[0].value = 0` |
| 名称含空白 | `name: "  idx\t"` | 解析后被 `trim` 为 `"idx"` |

### 3.6 异常场景

| 用例 | 输入 | 当前行为 | 期望行为 |
| --- | --- | --- | --- |
| 缺开始节点 | 无 `node_start` | 抛 `NoSuchElementException` | 应抛业务异常并提示「缺少开始节点」 |
| 缺结束节点 | 无 `node_end` | 抛 `NoSuchElementException` | 应抛业务异常并提示「缺少结束节点」 |
| `cells` 为空 | `{}` | NPE | 应抛业务异常 |
| 非法 JSON | `"abc"` | 反序列化异常 | 应抛业务异常并提示「DSL 格式非法」 |

> 最后三行对应 [项目现状](10-项目现状.md) 缺陷 P3，属**待修复项**。

---

## §4 接口验证

### 4.1 算子注册三接口

**保存（`POST /registerOperator/save`）**

```bash
curl -X POST "http://localhost:8082/servicearrange/registerOperator/save" \
     -H "Content-Type: application/json" \
     -d '{
       "type": "http",
       "serviceName": "百度搜索",
       "serviceUrl": "www.baidu.com",
       "requestMethod": "post",
       "description": "测试登记",
       "reqBodyType": "json",
       "reqBodyIsJsonSchema": true,
       "resBodyType": "json",
       "resBodyIsJsonSchema": true
     }'
```

| 用例 | 期望 |
| --- | --- |
| 正常保存 | 返回 `"success"`；`REGISTER_INFO` 新增一条且 `createDate` 已填 |
| 带 `id` 保存 | 更新既有记录，`modifyDate` 刷新 |
| 缺 `serviceName` | 当前仍会保存成功（无必填校验）→ 建议补校验 |

**查询（`GET /registerOperator/findById?id=`）**

| 用例 | 期望 |
| --- | --- |
| id 存在 | 返回完整实体 JSON |
| id 不存在 | 返回 `null`（HTTP 200）|
| 不传 id | 依实现返回 `null` 或报错 |

**删除（`POST /registerOperator/delete`）**

```bash
curl -X POST "http://localhost:8082/servicearrange/registerOperator/delete" \
     -H "Content-Type: application/json" \
     -d '["6650f3a1c2b3d4e5f6a7b8c9"]'
```

| 用例 | 期望 |
| --- | --- |
| 删除存在的 id | 返回 `"success"`，记录消失 |
| 删除不存在的 id | 返回 `"success"`（幂等）|
| 传空数组 | 返回 `"success"` |

### 4.2 运行实例接口

```bash
# 运行
curl -X POST "http://localhost:8082/servicearrange/inst/run/or/getResult/PLAN_001" \
     -F "servea_optType=run" \
     -F "servea_sync=false" \
     -F "name=张三" \
     -F "age=28"

# 查询结果
curl -X POST "http://localhost:8082/servicearrange/inst/run/or/getResult/PLAN_001" \
     -F "servea_optType=get_result"
```

| 用例 | 期望 | 备注 |
| --- | --- | --- |
| `run` | 返回 `instId`；`SERVEA_INST` 新增文档，`state=RUNNING` | — |
| 请求头采集 | `Inst.headerParams` 含全部请求头 | 用 `-H "X-AToken: abc"` 验证 |
| 入参类型还原 | `age` 落库为数字 `28` 而非字符串 | 依赖 `inputsJsonSchema` |
| `get_result` 且成功 | 返回出参 | 需流程真正跑通 |
| `get_result` 且未成功 | HTTP 500 + `实例还未运行成功，无输出信息！` | 当前行为 |
| 查历史实例 | **不支持**，`servea_instId` 被忽略 | 缺陷 P2 |

### 4.3 前置条件

运行接口测试前必须：

1. 把目标 DSL 覆盖到 `main/src/main/resources/hrh_http.json`；
2. 重启后端。

> 因缺转换器，流程会在第 2 个节点失败。要验证「完整跑通」，需先完成 [项目现状](10-项目现状.md) 阶段一任务 1~2。

---

## §5 DSL 样例验证

前端内置 3 个示例，可用于回归验证。

### 5.1 示例 1：开始 → 数据映射 → HTTP → 结束

| 校验项 | 期望 |
| --- | --- |
| 前端校验 | ✅ 通过（0 错误）|
| 节点数 | 4 个节点 + 3 条 `edge_common` |
| 开始节点 | `inputsJsonSchema` 声明 `name`（string）/ `age`（integer）|
| 数据映射 | `childInputs.reqQuery` 含 `name` → `#start_$name` |
| 导出 DSL | 无 `x`/`y` 字段；`cells` 长度 7 |
| 后端解析 | `DslParser` 可成功构建 `Inst` |
| 后端执行 | ❌ 缺 `node_inner_datamap_task`，第 2 节点失败 |

### 5.2 示例 2：带补偿的流程

| 校验项 | 期望 |
| --- | --- |
| 前端校验 | ✅ 通过 |
| 强组合 | HTTP 节点 `groupIds` 含组 id；导出后 `groups[0].nodes` 已反向填充 |
| 补偿线 | 存在 1 条 `edge_compensate` |
| 后端解析 | 可构建，`groups` 正确填充 |
| 后端执行 | ❌ 缺补偿算子执行器与转换器 |

### 5.3 示例 3：doWhile 循环 + 决策线

| 校验项 | 期望 |
| --- | --- |
| 前端校验 | ✅ 通过（可能含「无退出条件」类提示，取决于表达式是否填写）|
| 循环线 | 1 条 `edge_loop`，`loopType=doWhile`，`cycleInfo.jsonpathSelect=#dynamicParams_$idx` |
| 决策线 | 1 条 `edge_decision`，`jsonpathElexpression=R0<3` |
| 全局参数 | 1 个 `global_Data` 节点，含动态参数 `idx` |
| 后端解析 | 可构建，`loopRunTimesMap` 含该循环线的 `NodeLoopInfo` |
| 区间推导 | 区间含决策节点与 HTTP 节点 |
| 后端执行 | ❌ 缺转换器；且决策线判断逻辑失效（缺陷 P0）|

### 5.4 前端校验规则用例

| 用例 | 构造方式 | 期望 |
| --- | --- | --- |
| R1 缺结束节点 | 从节点库删掉「结束」（若不存在则只留开始）| 报错「缺少结束节点」 |
| R1 重复开始节点 | 尝试拖入第二个「开始」| 被拦截并提示 |
| R4 节点无入线 | 拖入孤立节点 | 报错「没有入线」 |
| R4 节点无出线 | 拖入孤立节点 | 报错「没有出线」 |
| R6 结束节点有出线 | 从结束节点拖线 | 被拦截并提示 |
| R7 决策线无表达式 | 拖决策线后清空表达式 | 报错「缺少判定表达式」 |
| R7 循环线无条件 | 循环线清空条件 | 提示「未设置循环条件（将无限循环）」 |
| R8 引用不存在的组 | 手工构造 `groupIds` 指向不存在组 | 报错「引用了不存在的强组合」 |
| R9 非法 Schema | 开始节点 `inputsJsonSchema` 填 `{` | 报错「入参 Schema 非法」 |
| 可达性 | 构造不可达节点 | 提示「不可达」 |
| 补偿组未挂补偿线 | 加入补偿组但不画补偿线 | 提示「属于补偿组，但没有挂接补偿线」 |

---

## §6 缺陷复现与验证

每个缺陷给出「复现步骤」与「修复后的验证方法」。

### 6.1 P0-1：决策线失效

| 项 | 内容 |
| --- | --- |
| 位置 | `TaskResultConsumer` L381-385 |
| 复现 | 构造含决策线的流程（如示例 3），运行后观察：即使表达式为真，目标节点仍被置 `SKIP` |
| 根因 | `dicisionEdges` 在 L381 初始化为 `null`，L384 的 `stream().collect()` 返回值未赋值给它；L396 条件恒假 |
| 修复 | 补上赋值：`dicisionEdges = inputEdges.stream()...collect(...)` |
| 验证 | 表达式为真 → 目标节点正常执行；为假 → 目标节点置 `SKIP` |

### 6.2 P0-2：转换器缺失

| 项 | 内容 |
| --- | --- |
| 位置 | `convert` 包 |
| 复现 | 运行任何「开始 → 非结束节点」的流程，日志出现 `getBean` 失败或 NPE |
| 根因 | 容器中只有 `node_start_task` / `node_end_task` 两个 Bean，而调度层对下一节点统一取 `cellType + "_task"` |
| 修复 | 按 [算子扩展指南](07-算子扩展指南.md) §3 步骤 3 补齐 |
| 验证 | 补齐后日志无 `getBean` 异常，节点任务正常创建 |

### 6.3 P1-1：DSL 硬编码

| 项 | 内容 |
| --- | --- |
| 位置 | `InstController` L80 |
| 复现 | 用任意 `planId` 调用运行接口，实际执行的始终是 `hrh_http.json` 的内容 |
| 根因 | `FileUtil.readUtf8String("hrh_http.json")` 固定读 classpath 资源 |
| 修复 | 引入 `Plan` 实体，按 `planId` 查 DSL |
| 验证 | 两个不同的 `planId` 分别执行不同的流程 |

### 6.4 P1-2：whileDo NPE

| 项 | 内容 |
| --- | --- |
| 位置 | `TaskResultConsumer` L334 |
| 复现 | 构造 `loopType=whileDo` 的流程并运行 |
| 根因 | 该分支内 `outLoopEdges` 可能为 `null`，却直接调用 `.get(0)` |
| 修复 | 使用前判空，或把循环线集合获取提前到分支外 |
| 验证 | whileDo 流程可正常循环并跳出，无 NPE |

### 6.5 P2-1：SKIP 传播判定

| 项 | 内容 |
| --- | --- |
| 位置 | `TaskResultConsumer` L389 |
| 复现 | 构造「父节点全部 SKIP」的场景，观察子节点是否被正确置 SKIP |
| 根因 | 判定写作 `pSkipNum == skipNodes.size()`（实例全局 skip 数），语义应为 `pids.size()`（父节点数）|
| 修复 | 改为与父节点数量比较 |
| 验证 | 父全 SKIP → 子 SKIP；父部分 SKIP → 子不 SKIP |

### 6.6 P2-2：MQ 未真正投递

| 项 | 内容 |
| --- | --- |
| 位置 | `TaskProductor` L42 / L49 |
| 复现 | RabbitMQ 管理台观察：任务执行期间 `/task/run` 队列无消息入队 |
| 根因 | `sendTaskRun` / `sendTaskResult` 为本地方法直调 |
| 修复 | 改用 `MqChannelProcessor` 已定义的通道真正投递 |
| 验证 | 管理台可见消息流转；部署 2 个后端实例，任务可跨节点处理 |

### 6.7 P2-3：取值分支疑似笔误

| 项 | 内容 |
| --- | --- |
| 位置 | `DataMapOperateExecutor` L283 |
| 复现 | 构造上游节点取值（`#pno_xxx$field`）场景 |
| 根因 | `jsonObject.equals(expression)` 疑似应为 `jsonObject.containsKey(...)` |
| 修复 | 按意图修正 |
| 验证 | 上游节点取值能正确命中 |

---

## §7 测试清单

### 7.1 部署后冒烟（10 项）

| # | 检查项 | 通过标准 |
| --- | --- | --- |
| 1 | 后端进程运行 | `Started ServiceArrangeApplication` |
| 2 | 端口监听 | `:8082` LISTENING |
| 3 | Mongo 连通 | `SERVEA_INST` 可读 |
| 4 | MQ 连通 | 交换器 `servicearrange-exchange-point` 存在 |
| 5 | ZK 连通 | 客户端可连接 |
| 6 | 算子接口 | `findById` 返回 200 |
| 7 | 算子保存 | 返回 `success` |
| 8 | 算子删除 | 返回 `success` |
| 9 | 前端加载 | 页面正常渲染，节点库 24 项 |
| 10 | 前端连通性检测 | 顶栏显示「已连通」 |

### 7.2 功能回归（20 项）

| # | 检查项 |
| --- | --- |
| 1~3 | 3 个示例流程均可加载且校验通过 |
| 4~6 | DSL 导入 / 导出 / 校验三项功能正常 |
| 7~9 | 节点拖拽新增 / 移动 / 删除正常 |
| 10~12 | 连线创建 / 线型切换 / 重复连线拦截正常 |
| 13~14 | 属性面板随节点类型动态生成、字段可编辑并持久化 |
| 15 | 补偿组加入 / 退出正常 |
| 16 | 画布缩放 / 平移 / 适应正常 |
| 17 | 运行实例入参表单按 Schema 自动生成 |
| 18 | Schema 工具生成结果符合规范 |
| 19 | 算子注册表单 20 字段可保存与回填 |
| 20 | 模型本地持久化（刷新页面后仍在）|

### 7.3 待补自动化用例（建议）

| 优先级 | 测试类 | 覆盖 |
| --- | --- | --- |
| P1 | `DslParserTest` | §3 全部用例 |
| P1 | `DslValidatorTest`（前端或移植到 JS 测试框架） | §5.4 全部用例 |
| P2 | `TaskServiceExpressionTest` | 表达式求值、R/L 替换、字符串加引号 |
| P2 | `JsonSchemaUtilTest` | Schema → 值、类型还原边界 |
| P3 | `DataMapOperateExecutorTest` | 五类映射 + 取值表达式分支 |
| P3 | `InstControllerIT` | 接口集成测试（需 testcontainers）|

---

## 📚 相关文档
- 🗺️ [项目现状](10-项目现状.md) — 缺陷清单（本文 §6 的复现对象）
- ✅ [功能清单](09-功能清单.md) — 测试覆盖度的参照
- 🚀 [快速开始](11-快速开始.md) — 环境搭建
- 🔗 [DSL规范](02-DSL规范.md) — §7 校验规则（前端校验用例来源）
- 📝 [REST接口说明](05-REST接口说明.md) — 接口参数细节
- ⚙️ [执行引擎与调度流程](03-执行引擎与调度流程.md) — 调度链路与算法
