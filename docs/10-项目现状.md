# 项目现状

本文回答一个具体问题：**当前这份代码，哪些能跑、哪些不能跑、差在哪里**。所有结论均来自对 `main` 分支源码的逐行核对，附行号可查证。

> 结论先行：**流程骨架（解析 → 调度）基本完整，但算子层严重缺失**。24 种节点类型中只有 3 种真正可用，导致除「开始 → 数据映射 → HTTP → 结束」这一条最小链路外的绝大部分流程无法跑通。

---

## 📋 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 项目现状 |
| 文档版本 | v1.0 |
| 最后更新 | 2026-09-14 |
| 状态 | 现行 |
| 代码基线 | `main` 分支，54 个 Java 文件 + `frontend/` |
| 相关文档 | [功能清单](09-功能清单.md) · [系统架构](15-系统架构.md) |

---

## 目录

- [§1 代码地图](#§1-代码地图)
- [§2 可用性判定](#§2-可用性判定)
- [§3 缺陷清单](#§3-缺陷清单)
- [§4 设计与实现差距](#§4-设计与实现差距)
- [§5 演进路线](#§5-演进路线)

---

## §1 代码地图

### 1.1 模块与文件分布

| 模块 | Java 文件数 | 职责 | 是否可独立运行 |
| --- | --- | --- | --- |
| `backend/engine` | 约 46 | 核心引擎（解析/转换/执行/编排/持久化） | ❌ 纯库模块 |
| `backend/web` | 约 8 | Web 接入层 + 启动类 + 配置 | ✅ Spring Boot 应用 |
| **合计** | **54** | | |

### 1.2 包级职责对照

| 包路径 | 文件数 | 职责 | 完整度 |
| --- | --- | --- | --- |
| `dsl` | 12 | DSL 数据结构（Cell 家族） | ★★★★☆ 主要节点齐全，sleep/dataresult 等缺独立 Cell |
| `entity` | 6 | 持久化实体 | ★★★★★ 完整 |
| `parser` | 4 | DSL 解析 + 类型常量 | ★★★★☆ 解析完整，健壮性待加强 |
| `executor` | 5 | 执行器接口 + 4 个实现 | ★☆☆☆☆ **仅 3 种算子可用** |
| `convert` | 3 | Cell → Task 转换器 | ★☆☆☆☆ **仅 2 个实现** |
| `mq` | 5 | 消息生产/消费/通道 | ★★★☆☆ 链路通，但未真正走 MQ |
| `dao` | 4 | Mongo 数据访问 | ★★★★★ 完整 |
| `service` | 4 | 业务服务 | ★★★★☆ 求值引擎完整 |
| `config` | 2 | Mongo / Zookeeper 配置 | ★★★★★ 完整 |
| `utils` | 2 | 工具类 | ★★★★☆ `JsonSchemaUtil` 完整 |
| `vo` | 3 | 出入参对象 | ★★★★★ 完整 |
| `controller`（main） | 2 | REST 接口 | ★★★☆☆ 4 个接口，均有限制 |

---

## §2 可用性判定

### 2.1 算子可用性（核心结论）

| cellType | 显示名 | `Execute` 实现 | `TaskInterface` 实现 | 实际可用 |
| --- | --- | --- | --- | --- |
| `node_start` | 开始 | ✅ `StartOperateExecutor` | ✅ `StartTask` | ✅ **可用** |
| `node_end` | 结束 | ✅ `EndOperateExecutor` | ✅ `EndTask` | ✅ **可用** |
| `node_inner_datamap` | 数据映射 | ✅ `DataMapOperateExecutor` | ❌ 缺 | ❌ **不可用** |
| `node_outer_http` | HTTP | ✅ `HttpTaskExecutor` | ❌ 缺 | ❌ **不可用** |
| `node_inner_sleep` | 延时 | ❌ | ❌ | ❌ 不可用 |
| `node_inner_decision` | 决策 | ❌ | ❌ | ❌ 不可用 |
| `node_inner_dataresult` | 数据结果 | ❌ | ❌ | ❌ 不可用 |
| `node_inner_transform2Obj` | 规则转换 | ❌ | ❌ | ❌ 不可用 |
| `node_inner_compensateDatamap` | 补偿数据映射 | ❌ | ❌ | ❌ 不可用 |
| `node_inner_convertJson2Xml` | JSON→XML | ❌ | ❌ | ❌ 不可用 |
| `node_inner_convertXml2Json` | XML→JSON | ❌ | ❌ | ❌ 不可用 |
| `node_inner_execShell` | Shell | ❌ | ❌ | ❌ 不可用 |
| `node_inner_execJar` | Jar | ❌ | ❌ | ❌ 不可用 |
| `node_inner_execPython` | Python | ❌ | ❌ | ❌ 不可用 |
| `node_outer_dubbo` | Dubbo | ❌ | ❌ | ❌ 不可用 |
| `node_outer_webservice` | WebService | ❌ | ❌ | ❌ 不可用 |
| `node_outer_httpCompensate` | HTTP 补偿 | ❌ | ❌ | ❌ 不可用 |
| `node_outer_dubboCompensate` | Dubbo 补偿 | ❌ | ❌ | ❌ 不可用 |
| `node_outer_webserviceCompensate` | WebService 补偿 | ❌ | ❌ | ❌ 不可用 |
| `global_Data` | 全局参数 | —（解析时抽出） | — | ⚠️ 仅参与参数注入 |

**统计：24 种类型中 2 种完全可用，1 种半可用（有执行器无转换器），21 种不可用。**

### 2.2 为什么「有执行器」仍不可用

调度层对每个下一批节点都执行（`TaskResultConsumer` L365）：

```java
TaskInterface ti = SpringUtil.getBean(cell.getCellType() + TaskType.suffix);
Task t = ti.convert(cell, inst);
```

`TaskType.suffix = "_task"`。当前容器中只有两个 Bean：

| Bean 名 | 来源 |
| --- | --- |
| `node_start_task` | `StartTask` |
| `node_end_task` | `EndTask` |

因此**只要相邻节点不是「开始」或「结束」，取 Bean 即失败**。`node_inner_datamap` 与 `node_outer_http` 虽然有执行器，但没有转换器，依然跑不通。

同理，执行器侧（`TaskRunConsumer` L68-72）：

```java
if (Arrays.asList(CellType.FUN_JAR, CellType.FUN_PYTHON).contains(task.getType())) {
    execute = (Execute) SpringBeanUtils.getBean(CellType.FUN_SHELL);
} else {
    execute = (Execute) SpringBeanUtils.getBean(task.getType());
}
```

取不到 Bean 时返回 `null`，后续 `execute.run(...)` 直接 NPE。

### 2.3 端到端可用性结论

| 场景 | 可用性 | 原因 |
| --- | --- | --- |
| 开始 → 结束 | ✅ 可用 | 两个转换器与执行器都在 |
| 开始 → 数据映射 → HTTP → 结束 | ❌ 不可用 | 缺 `node_inner_datamap_task` / `node_outer_http_task` |
| 任意含条件分支的流程 | ❌ 不可用 | 决策线判断逻辑失效（缺陷 #1）+ 缺转换器 |
| 任意含循环的流程 | ❌ 不可用 | 缺转换器；whileDo 另有 NPE 风险 |
| 任意含补偿的流程 | ❌ 不可用 | 缺补偿算子执行器与转换器 |
| 按 `planId` 运行不同流程 | ❌ 不可用 | DSL 硬编码读 `hrh_http.json` |

> **实践建议**：若要在当前代码上验证前端产出的 DSL，只能验证到「解析是否成功」这一步（`DslParser.parser` 可独立调用），无法跑通完整执行链路。

---

## §3 缺陷清单

按优先级排序。**P0 = 功能完全不可用；P1 = 阻塞主链路；P2 = 特定场景出错；P3 = 健壮性/可维护性。**

| 优先级 | 位置 | 缺陷描述 | 影响范围 | 修复建议 |
| --- | --- | --- | --- | --- |
| **P0** | `TaskResultConsumer` L381-385 | `inputEdges.stream()...collect(Collectors.toList())` 的返回值**未被接收**；`dicisionEdges` 在 L381 被初始化为 `null` 后从未赋值 | 决策线判断恒为假 → 条件分支功能完全不可用 | 把 `dicisionEdges = inputEdges.stream()...` 的赋值补上 |
| **P0** | `convert` 包 | 24 种节点类型中只有 2 个 `TaskInterface` 实现（`node_start_task` / `node_end_task`），缺 22 个 | 除首尾节点外无法转任务，绝大多数节点流转时抛异常 | 按 [算子扩展指南](07-算子扩展指南.md) §3 步骤 3 逐个补齐 |
| **P1** | `executor/impl` 包 | 24 种节点类型中只有 4 个 `Execute` 实现，缺 20 个 | 补偿/Dubbo/WebService/脚本/JSON↔XML 等算子全不可用 | 按 [算子扩展指南](07-算子扩展指南.md) §3 步骤 4 补齐 |
| **P1** | `InstController` L80 | DSL 硬编码 `FileUtil.readUtf8String("hrh_http.json")`，未使用 `planId` | 无法按流程模型运行；多流程无法共存 | 引入 `Plan` 实体与集合，按 `planId` 查 DSL |
| **P1** | `TaskResultConsumer` L334 | whileDo 分支内使用 `outLoopEdges.get(0)`，而该分支中 `outLoopEdges` 可能为 `null` | whileDo 循环触发 NPE | 先判空，或把循环线集合的获取提前到分支外 |
| P2 | `InstController` L178-189 | `get_result` 分支不使用 `servea_instId`，只查本次请求新建的实例 | 无法查询历史实例结果 | 读入 `servea_instId` 并按 id 查库 |
| P2 | `InstController` L210-229 | 文件下载分支中 `fileName` 可能为 `null` 即 `URLEncoder.encode` | 非 JSON 出参且路径为 `filemgr://` 时 NPE | 补判空；并实现下载逻辑（当前为 TODO） |
| P2 | `TaskResultConsumer` L389 | `pSkipNum == skipNodes.size()`，语义上应为 `pSkipNum == pids.size()` | SKIP 传播判断可能失效，导致下游节点误执行或漏执行 | 改为与父节点数量比较 |
| P2 | `TaskProductor` L42 / L49 | `sendTaskRun` / `sendTaskResult` 实为**本地方法直调**，未投递 MQ | 多实例部署时任务不会跨节点分发，无法水平扩容 | 改用 `MqChannelProcessor` 中已定义的通道真正投递 |
| P2 | `DataMapOperateExecutor` L283 | `jsonObject.equals(expression)` 疑似应为 `jsonObject.containsKey(...)` | 上游节点取值分支可能失效 | 按意图修正为 `containsKey` |
| P3 | `DslParser` L261 / L278 | 递归出口用 `static` 计数器 `Chilednum` / `Parentnum` | 并发调用 `parser` 时结果错误 | 改为方法参数传递 |
| P3 | `DslParser` L33 / L139 / L152 | `Optional.get()` 未判空 | DSL 缺首尾节点时抛 `NoSuchElementException`，错误信息不友好 | 加 `orElseThrow(() -> new BizException("缺少开始节点"))` |
| P3 | `TaskType` L25-47 | 除 `START` / `END` 外常量被整体注释 | 可读性差，全靠 `cellType + "_task"` 约定，易出错 | 恢复常量定义并在 `convert` 中显式声明 |
| P3 | `HttpTaskExecutor` L140-147 | 文件下载为 TODO，`fileList` 恒为空 | 文件上传功能不可用 | 实现文件读写 |
| P3 | `InstLog` | 日志级别常量拼写为 `LEVEL_ERRO`（缺 `R`） | 命名不规范，易误用 | 重命名并保留兼容别名 |
| P3 | `Inst` | 时间字段拼写为 `starDate`（应为 `startDate`） | 字段名错误，前端需特判 | 重命名（注意 Mongo 已有数据兼容） |
| P3 | `RegisterInfoEntity` | 字段 `sheelFtpPath`（应为 `shellFtpPath`） | 拼写错误 | 重命名 |
| P3 | `Inst.syncInstMap` | 静态 Map 已定义但从未使用 | 死代码 | 删除或落实同步等待逻辑 |

---

## §4 设计与实现差距

### 4.1 设计意图 vs 当前实现

| 设计意图 | 设计文档 | 当前实现 | 差距 |
| --- | --- | --- | --- |
| 24 种节点类型全覆盖 | [DSL规范](02-DSL规范.md) §3 | 仅 2 种可端到端运行 | **大** |
| 消息驱动、可水平扩容 | [执行引擎与调度流程](03-执行引擎与调度流程.md) §1 | 本地直调，通道定义了但未用 | **大** |
| 失败自动补偿、局部事务回滚 | [项目总览](01-项目总览.md) §4 | 补偿触发链路完整，但补偿算子未实现 | **大** |
| doWhile / whileDo 双循环模式 | [DSL规范](02-DSL规范.md) §4.4 | doWhile 逻辑可用，whileDo 有 NPE | 中 |
| 决策线条件分支 | [DSL规范](02-DSL规范.md) §4.3 | 求值引擎完整，但接线错误导致失效 | **大** |
| 前端可视化编排 | [前端控制台说明](06-前端控制台说明.md) | 完整实现，可导出可用 DSL | 无 |
| 流程模型（Plan）管理 | [系统架构](15-系统架构.md) §3 | 无 Plan 实体、无 CRUD 接口 | **大** |
| 实例可观测（列表/轨迹/日志查询）| [系统架构](15-系统架构.md) §8 | 仅日志落库，无查询接口 | 中 |
| 分布式锁保证实例串行 | [数据模型与消息模型](04-数据模型与消息模型.md) §10 | ZooKeeper 锁实现完整可用 | 无 |
| 统一返回结构 `{code,msg,data}` | [REST接口说明](05-REST接口说明.md) §8 | 未实现，异常直接抛出 | 中 |

### 4.2 未被使用的已定义能力

以下代码已写好但**当前无调用方**，属于「躺在仓库里的半成品」：

| 位置 | 内容 | 说明 |
| --- | --- | --- |
| `MqChannelProcessor` | 4 个 MQ 通道定义 | 通道名已定义，无生产者/消费者真正绑定 |
| `Inst.syncInstMap` | 同步实例内存缓存 | 定义后未使用 |
| `Task.debug` | 调试运行字段 | 字段与 `StartOperateExecutor` 中的 debug 分支已存在，但 Controller 未开放 `optType=debug` |
| `Inst.STATE_RESUME` | 恢复状态 | 常量与挂起检查已实现，但无恢复入口 |
| `Task.rollbackState` | 回滚状态字段 | 定义后未使用 |
| `Task.RetryRules.doneTimes` | 已重试次数 | 定义后未使用（重试逻辑未实现） |
| `DslParser.getDslStartParamsTypeMap` | 入参类型映射 | 已被 `InstController` 使用，但仅覆盖 4 类类型 |

---

## §5 演进路线

按「先让主链路可用，再补齐能力，最后做工程化」的顺序推进。

### 阶段一：打通主链路（P0/P1，最高优先）

| 序号 | 任务 | 依赖 | 验收标准 |
| --- | --- | --- | --- |
| 1 | 修复决策线 `dicisionEdges` 赋值缺陷 | — | 决策线表达式为假时目标节点被置 SKIP，为真时正常执行 |
| 2 | 补齐 5 种核心节点的 `TaskInterface` | — | `node_inner_datamap` / `node_outer_http` / `node_inner_decision` / `node_inner_sleep` / `node_inner_dataresult` 均可转任务 |
| 3 | 修复 whileDo 的 `outLoopEdges` NPE | — | `loopType=whileDo` 的流程可正常循环与跳出 |
| 4 | 引入 `Plan` 实体与 CRUD 接口 | — | `POST /plan/save` 保存 DSL；`InstController` 按 `planId` 读取 DSL |
| 5 | 修复 SKIP 传播判定条件 | #1 | 父节点全 SKIP 时子节点也 SKIP |

**阶段一完成后的可用范围**：开始 → 数据映射 → HTTP → 决策 → 结束，含 doWhile 循环。

### 阶段二：补齐算子（P1/P2）

| 序号 | 任务 | 说明 |
| --- | --- | --- |
| 6 | 实现 Shell / Jar / Python 执行器 | 三者复用同一 `Execute`，路由已在 `TaskRunConsumer` 中预留 |
| 7 | 实现规则转换（`transform2Obj`）执行器与转换器 | 支撑数据重组场景 |
| 8 | 实现 JSON↔XML 互转 | 支撑异构系统对接 |
| 9 | 实现补偿数据映射 + HTTP 补偿算子 | 与补偿触发链路打通，形成完整回滚能力 |
| 10 | 实现 Dubbo / WebService 及其补偿算子 | 需引入对应 SDK 与配置 |

### 阶段三：工程化（P2/P3）

| 序号 | 任务 | 说明 |
| --- | --- | --- |
| 11 | `TaskProductor` 改为真正投递 MQ | 启用 `MqChannelProcessor` 通道，支持多实例部署 |
| 12 | 引入 `@ControllerAdvice` 与统一返回结构 | 对齐 [REST接口说明](05-REST接口说明.md) §8 建议契约 |
| 13 | 补齐实例列表 / 详情 / 日志查询接口 | 支撑前端运行监控与轨迹回放 |
| 14 | 修复 `DslParser` 线程安全与判空问题 | 递归计数器改参数传递；`Optional` 加异常提示 |
| 15 | 修正命名拼写（`starDate` / `LEVEL_ERRO` / `sheelFtpPath`）| 注意 Mongo 历史数据兼容 |
| 16 | 实现实例挂起/恢复 | 落实 `STATE_SUSPEND` / `STATE_RESUME` 语义 |
| 17 | 实现重试逻辑 | 落实 `RetryRules.doneTimes` / `delaySecondTimes` |
| 18 | 增加循环最大次数保护 | 防止条件写错导致无限循环 |
| 19 | 引入健康检查、限流熔断、链路追踪 | 见 [系统架构](15-系统架构.md) §7 / §8 |
| 20 | 容器化与 CI/CD | 见 [部署指南](13-部署指南.md) §7 |

### 阶段四：能力增强（可选）

| 序号 | 任务 |
| --- | --- |
| 21 | 实例执行轨迹持久化与可视化回放 |
| 22 | 流程版本管理与灰度发布 |
| 23 | 子流程 / 子编排（流程复用） |
| 24 | 定时触发与事件触发（主动拉起实例） |
| 25 | 算子市场（基于 `RegisterInfoEntity` 的可视化选用） |

---

## 📚 相关文档
- ✅ [功能清单](09-功能清单.md) — 逐项功能的实现状态
- ⚙️ [执行引擎与调度流程](03-执行引擎与调度流程.md) — 缺陷所在的算法位置
- 🧩 [算子扩展指南](07-算子扩展指南.md) — 补齐算子实现的方法
- 🔗 [DSL规范](02-DSL规范.md) — 契约基线
- 🏗️ [系统架构](15-系统架构.md) — 设计意图与技术栈
- 🚀 [快速开始](11-快速开始.md) — 在当前代码上能验证到什么程度
