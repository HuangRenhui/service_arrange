# 数据模型与消息模型

本文说明服务编排引擎的持久化对象、MongoDB 集合、消息通道与分布式锁。

> 代码依据：`entity/*`、`dao/*`、`vo/*`、`mq/MqChannelProcessor.java`。

---

## 📋 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 数据模型与消息模型 |
| 文档版本 | v1.0 |
| 最后更新 | 2026-09-14 |
| 状态 | 现行 |
| 相关代码 | `entity/*.java` · `dao/*.java` · `vo/*.java` · `mq/MqChannelProcessor.java` |

---

## 目录

- [§1 数据模型总览](#§1-数据模型总览)
- [§2 BaseEntity](#§2-baseentity)
- [§3 Inst（实例）](#§3-inst实例)
- [§4 Task（任务）](#§4-task任务)
- [§5 InstLog（日志）](#§5-instlog日志)
- [§6 NodeLoopInfo（循环信息）](#§6-nodeloopinfo循环信息)
- [§7 RegisterInfoEntity（算子注册）](#§7-registerinfoentity算子注册)
- [§8 MongoDB 集合一览](#§8-mongodb-集合一览)
- [§9 消息模型](#§9-消息模型)
- [§10 分布式锁](#§10-分布式锁zookeeper)
- [§11 应用配置](#§11-应用配置applicationyml)

---

## §1 数据模型总览

```
             ┌───────────────┐
             │     Inst      │  一次流程运行的完整上下文（内存图 + 状态集合 + 出参）
             │  SERVEA_INST  │
             └──────┬────────┘
                    │ 1 : N
             ┌──────▼────────┐
             │     Task      │  某节点在某循环次数下的一次执行记录
             │  SERVEA_TASK  │
             └──────┬────────┘
                    │ 1 : N
             ┌──────▼────────┐
             │    InstLog    │  实例/节点级日志
             │ SERVEA_INST_LOG│
             └───────────────┘

             ┌──────────────────────┐
             │  RegisterInfoEntity  │  算子注册（外部 API 元信息）
             │    REGISTER_INFO     │
             └──────────────────────┘
```

---

## §2 `BaseEntity`

所有实体的公共父类（`entity/BaseEntity.java`）：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | `String` | 主键，MongoDB `_id` |
| `createDate` | `Date` | 创建时间 |
| `modifyDate` | `Date` | 修改时间 |
| `remark` | `String` | 备注 |

---

## §3 `Inst`（实例）

一次流程运行的全部上下文。`@Document` 集合名 `SERVEA_INST`。

### 3.1 静态常量（真实定义）

| 常量 | 值 | 说明 |
| --- | --- | --- |
| `STATE_WAITE` | `WAITE` | 等待（实例创建完未开始）|
| `STATE_RUNNING` | `RUNNING` | 运行中 |
| `STATE_SUCCESS` | `SUCCESS` | 运行成功 |
| `STATE_FAIL` | `FAIL` | 运行失败 |
| `STATE_SUSPEND` | `SUSPEND` | 暂停（当前节点执行完后续节点暂停）|
| `STATE_RESUME` | `RESUME` | 恢复（对暂停节点重新启动）|
| `ZK_LOCK_PREFIX` | `/INST_LOCK_` | ZooKeeper 实例锁路径前缀 |
| `syncInstMap` | `Map<String, Inst>` | 同步实例内存缓存（`@Transient`，当前未被使用）|

### 3.2 业务字段（真实定义 + `@Field` 映射）

| 字段 | 类型 | `@Field` | 说明 |
| --- | --- | --- | --- |
| `planId` | `String` | `planId` | 所属流程（模型）id |
| `dsl` | `String` | `dsl` | 原始 DSL JSON 快照 |
| `optType` | `String` | `optType` | 操作类型：`run` / `debug` / `get_result`，默认 `"run"` |
| `sync` | `Boolean` | `sync` | 是否同步，默认 `false` |
| `dslInputParams` | `String` | `dslInputParams` | 流程入参（JSON 字符串）⚠️ 非 `inputs` |
| `headerParams` | `Map<String,String>` | `headerParams` | 请求头集合 ⚠️ 非 `header`（且为 Map 非 String）|
| `staticParams` | `List<KeyValueDto>` | `staticParams` | 静态全局参数（常量）|
| `dynamicParams` | `List<KeyValueDto>` | `dynamicParams` | 动态全局参数（变量，循环计数等）|
| `nodeMap` | `Map<String, Cell>` | `nodeMap` | id → Cell（含连线）|
| `roots` | `Set<String>` | `roots` | 入口节点集合（解析后只含 `startId`）|
| `nodeChildsMap` | `Map<String, Set<String>>` | `nodeChildsMap` | 节点 → 子节点 |
| `nodeParentsMap` | `Map<String, Set<String>>` | `nodeParentsMap` | 节点 → 父节点 |
| `nodeInputEdgesMap` | `Map<String, Set<String>>` | `nodeInputEdgesMap` | 节点 → 入度连线 |
| `nodeOutEdgesMap` | `Map<String, Set<String>>` | `nodeOutEdgesMap` | 节点 → 出度连线 |
| `state` | `String` | `state` | 实例状态，默认 `"WAITE"` |
| `totalNodes` | `Set<String>` | `totalNodes` | 全部节点 id |
| `waitingNodes` | `Set<String>` | `waitingNodes` | 待执行节点 |
| `successNodes` | `Set<String>` | `successNodes` | 成功节点 |
| `failNodes` | `Set<String>` | `failNodes` | 失败节点 |
| `skipNodes` | `Set<String>` | `skipNodes` | 跳过节点 |
| `suspendNodes` | `Set<String>` | `suspendNodes` | 挂起时被阻塞的 `节点名\|循环次数` |
| `outputs` | `Task.Result` | `outputs` | 实例出参 |
| `starDate` | `Date` | `starDate` | 开始时间 ⚠️ **拼写为 `starDate`（非 `startDate`）** |
| `endDate` | `Date` | `endDate` | 结束时间 |
| `groups` | `List<Group>` | `groups` | 强组合（补偿组）|
| `loopRunTimesMap` | `Map<String, NodeLoopInfo>` | `loopRunTimesMap` | 循环线 id → 循环信息 |

---

## §4 `Task`（任务）

一个节点在某个循环次数下的一次执行记录。集合名 `SERVEA_TASK`。

### 4.1 状态常量

| 常量 | 值 | 说明 |
| --- | --- | --- |
| `STATE_WAITE` | `WAITE` | 等待（`convert` 后的初始状态）|
| `STATE_RUNNING` | `RUNNING` | 执行中 |
| `STATE_SUCCESS` | `SUCCESS` | 成功 |
| `STATE_SKIP` | `SKIP` | 跳过（父节点跳过 / 决策不通过）|
| `STATE_FAIL` | `FAIL` | 失败 |

### 4.2 字段（真实定义 + `@Field` 映射）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `instId` | `String` | 所属实例 |
| `planId` | `String` | 所属流程（模型）id |
| `planName` | `String` | 模型名称 |
| `nodeId` | `String` | 节点（Cell）id |
| `nodeName` | `String` | 节点名，为空时回退 `cellType_id` |
| `state` | `String` | 任务状态 |
| `type` | `String` | 任务类型 = `cellType`（Execute Bean 名）|
| `debug` | `Boolean` | 是否调试运行，默认 `false` |
| `inputs` | `String` | 节点入参 |
| `rollbackState` | `String` | 回滚状态 |
| `outputs` | `Task.Result` | 节点出参 |
| `retryRules` | `Task.RetryRules` | 重试规则 |
| `stateNackTimes` | `Integer` | 结果处理阶段 nack 次数（≥5 强制 ack），默认 `0` |
| `runNackTimes` | `Integer` | 执行阶段 nack 次数（≥5 强制 ack），默认 `0` |
| `startDate` | `Date` | 总开始时间 |
| `endDate` | `Date` | 总结束时间 |
| `executorStartDate` | `Date` | 执行器开始时间 |
| `executorEndDate` | `Date` | 执行器结束时间 |
| `loopTimes` | `String` | 循环标识，格式 `循环线id\|次数`，非循环节点为 `"0"` |

### 4.3 内嵌结构（真实定义）

**`Task.Result`**（节点返回值）：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `contentType` | `String` | 出参类型 |
| `jsonSchema` | `String` | 出参类型映射关系 ⚠️ 非 `body` |
| `value` | `String` | 出参值 ⚠️ 为 **String**，非对象 |
| `headerParams` | `Map<String,String>` | 出参头信息 |

**`Task.RetryRules`**（重试规则）：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `times` | `Integer` | 重试次数 |
| `doneTimes` | `Integer` | 已重试次数，默认 `0` ⚠️ 非 `interval` |
| `delaySecondTimes` | `Long` | 重试间隔（单位：秒）⚠️ 非 `condition` |

> 注意：`RetryRules` 中**没有** `condition` 字段，重试触发条件当前由执行器自行判断（`HttpTaskExecutor` 中通过 `error` 关键字匹配）。

### 4.4 关键索引（按使用方式推导）

| 索引 | 用途 |
| --- | --- |
| `{ instId, nodeId }` | `findAllByInstIdAndNodeIdIn`（决策求值取节点输出）|
| `{ instId, nodeId, loopTimes }` | `findByInstIdAndNodeIdAndLoopTimes`、`deleteByInstIdAndNodeIdAndLoopTimes` |

---

## §5 `InstLog`（日志）

集合名 `SERVEA_INST_LOG`。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| 字段 | 类型 | `@Field` | 说明 |
| --- | --- | --- | --- |
| `instId` | `String` | `instId` | 实例 id |
| `planId` | `String` | `planId` | 流程 id |
| `nodeId` | `String` | `nodeId` | 节点 id |
| `taskId` | `String` | `taskId` | 任务 id |
| `level` | `String` | `level` | 日志级别，默认 `INFO` |
| `message` | `String` | `message` | 日志内容 ⚠️ 字段名为 **`message`**，不是 `content` |

日志级别常量（注意源码拼写）：

| 常量 | 值 | 说明 |
| --- | --- | --- |
| `LEVEL_INFO` | `"INFO"` | 信息 |
| `LEVEL_WARN` | `"WARN"` | 警告 |
| `LEVEL_ERRO` | `"ERRO"` | 错误 ⚠️ 常量名与值均**缺少 `R`**（应为 `LEVEL_ERROR` / `"ERROR"`）|

**两个构造函数**（源码实际签名）：

```java
// 5 参：级别 + 内容
InstLog(String instId, String planId, String nodeId, String taskId, String level, String message)

// 5 参（重载）：只传级别，message 为 null
InstLog(String instId, String planId, String nodeId, String taskId, String level)

// 无参构造：会自动设置 createDate
InstLog()
```

实际用法示例：

```java
// 6 参 —— 级别 + 内容（推荐，信息最全）
instLogDao.save(new InstLog(task.getInstId(), task.getPlanId(), task.getNodeId(), task.getId(),
        InstLog.LEVEL_ERRO, "下标越界！" + e.getMessage()));

// 5 参 —— 仅级别（message 为 null，多见于 TaskProductor 的占位日志）
instLogDao.save(new InstLog(task.getInstId(), task.getPlanId(), task.getNodeId(), task.getId(),
        InstLog.LEVEL_INFO));
```

> ⚠️ **注意**：源码中**不存在**「5 参 = 内容」的构造。若要设置日志内容，必须使用 6 参形式并显式传级别。

---

## §6 `NodeLoopInfo`（循环信息）

不独立成集合，内嵌在 `Inst.loopRunTimesMap` 中。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `loopEdgeId` | `String` | 循环线 id |
| `idsBetweenLoopEdge` | `Set<String>` | 循环区间内的节点（含连线）|
| `times` | `int` | 已循环次数 |

---

## §7 `RegisterInfoEntity`（算子注册）

登记外部 API 元信息，供编排设计时复用。集合名 `REGISTER_INFO`。

### 7.1 字段（真实定义）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `type` | `String` | 类型：`http` / `rpc` 等 |
| `serviceName` | `String` | API 名称 |
| `serviceUrl` | `String` | 源路径 |
| `requestMethod` | `String` | 请求方法：`post` / `get` 等 |
| `description` | `String` | 描述 |
| `catalogueId` | `String` | 目录 id |
| `reqHeaders` | `String` | 入参 headers（JSON 字符串），含 Content-Type 等 |
| `reqQuery` | `String` | 入参 query / urlpath 参数（JSON 字符串）|
| `reqBodyType` | `String` | 入参 body 格式：`json` / `text` / `xml` |
| `reqBodyIsJsonSchema` | `Boolean` | 入参是否启用 JsonSchema |
| `reqBodyData` | `String` | 入参 body 数据（启用 Schema 后为 Schema 结构）|
| `resBodyIsJsonSchema` | `Boolean` | 出参是否启用 JsonSchema |
| `resBodyType` | `String` | 出参类型：`json` / 二进制流（HTML、图片、xml、json 文件）|
| `resBodyData` | `String` | 出参数据（json 时为 Schema）|
| `mockParam` | `String` | mock 测试参数 |
| `timeOut` | `String` | 超时设置 |
| `checkJsonSchema` | `String` | 服务编排校验用 Schema |
| `htmlPath` | `String` | html 路径 |
| `sheelFtpPath` | `String` | shell ftp 路径 |
| `id` / `createDate` / `modifyDate` / `remark` | — | 继承自 `BaseEntity` |

> ⚠️ **注意**：早期文档中的 `name` / `url` / `method` / `inputsJsonSchema` / `outputsJsonSchema` **均不存在**，实际为 `serviceName` / `serviceUrl` / `requestMethod` / `reqBodyData` / `resBodyData`。
> `createDate` / `modifyDate` 由 `RegisterInfoServiceImpl.save` 统一写入。

---

## §8 MongoDB 集合一览

| 集合名 | 实体 | 说明 |
| --- | --- | --- |
| `SERVEA_INST` | `Inst` | 实例 |
| `SERVEA_TASK` | `Task` | 任务 |
| `SERVEA_INST_LOG` | `InstLog` | 日志 |
| `REGISTER_INFO` | `RegisterInfoEntity` | 算子注册 |

### 8.1 DAO 接口

| 接口 | 继承 | 自定义方法 |
| --- | --- | --- |
| `InstDao` | `MongoRepository<Inst, String>` | `findStateById`（只取 `state` 字段）、`findSyncById`（只取 `sync`）、`findOutputsById`（只取 `outputs`）|
| `TaskDao` | `MongoRepository<Task, String>` | `deleteByInstIdAndNodeIdAndLoopTimes`、`findAllByInstIdAndNodeIdIn`、`findByInstIdAndNodeIdAndLoopTimes`、**`findStartTask`**（按 `instId + type` 查开始任务）|
| `InstLogDao` | `MongoRepository<InstLog, String>` | —（仅继承基础方法）|
| `RegisterInfoDao` | `MongoRepository<RegisterInfoEntity, String>` | `deleteAllByIdIn` |

**方法签名明细**：

```java
// InstDao —— 三个 @Query 使用 fields 投影，只回传必要字段
@Query(value = "{ 'id' : ?0 }", fields = "{ 'state' : 1 }")   Inst findStateById(String id);
@Query(value = "{ 'id' : ?0 }", fields = "{ 'sync' : 1 }")    Inst findSyncById(String id);
@Query(value = "{ 'id' : ?0 }", fields = "{ 'outputs' : 1 }") Inst findOutputsById(String id);

// TaskDao
void deleteByInstIdAndNodeIdAndLoopTimes(String instId, String nodeId, String loopTimes);
List<Task> findAllByInstIdAndNodeIdIn(String instId, Set<String> nodeIds);
Task findByInstIdAndNodeIdAndLoopTimes(String instId, String nodeId, String loopTimes);
@Query(value = "{ 'instId' : ?0 ,'type' : ?1 }")
Task findStartTask(String instId, String type);   // 供 EndOperateExecutor 取开始节点输出

// RegisterInfoDao
void deleteAllByIdIn(List<String> idList);
```

> `TaskDao` 源码中的注释提示：「XXX 当出现循环线后 一个节点可能生成多个任务，一些接口需要进行调整」——即 `findByInstIdAndNodeIdAndLoopTimes` 这类返回单对象的接口在循环场景下语义脆弱。

---

## §9 消息模型

### 9.1 通道定义（`MqChannelProcessor`）

Java 侧**只定义了 3 个**通道常量：

| 常量 | 通道名 | 方向 | 绑定方法 | 用途 |
| --- | --- | --- | --- | --- |
| `OUTPUT` | `output_channel` | `@Output` | `taskProductor()` | 通用产出通道 |
| `TASK_RUN_INPUT` | `task_run_input_channel` | `@Input` | `taskRunConsumer()` | 任务执行入队 |
| `TASK_RESULT_INPUT` | `task_result_input_channel` | `@Input` | `taskResultConsumer()` | 任务结果回传 |

```java
public interface MqChannelProcessor {
    String OUTPUT = "output_channel";
    String TASK_RUN_INPUT = "task_run_input_channel";
    String TASK_RESULT_INPUT = "task_result_input_channel";

    @Input(TASK_RESULT_INPUT)  SubscribableChannel taskResultConsumer();
    @Input(TASK_RUN_INPUT)     SubscribableChannel taskRunConsumer();
    @Output(OUTPUT)            MessageChannel taskProductor();
}
```

> **第 4 个通道**：`application.yml` 中额外配置了 `shell_task_status_channel`（消费者），但它**没有对应的 Java 常量与绑定方法**，且其 `group` 引用了未定义的 `${task.shell_task_status}`、`binding-routing-key` 存在 `${{` 语法错误。该通道当前无实际使用方。详见 [配置说明](12-配置说明.md) §3.3。

### 9.2 `Task4MQ`（任务消息体）

轻量消息，只带定位信息，消费端再查库：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | `String` | 任务 id |
| `type` | `String` | 任务类型（= cellType，用于选 Execute Bean）|

```json
{ "id": "6650f3...", "type": "node_outer_http" }
```

### 9.3 消息流转

```
TaskProductor.sendTaskRun(task)
   └─▶ Task4MQ{id,type} ──▶ task_run_input_channel ──▶ TaskRunConsumer
                                                        └─ getBean(type).run(taskId)

TaskRunConsumer 执行完（Execute 内部调用）
   └─▶ TaskProductor.sendTaskResult(task)
          └─▶ task_result_input_channel ──▶ TaskResultConsumer
                                              └─ 加 ZK 锁 → 编排下一批节点
```

### 9.4 ACK 策略

| 场景 | 动作 | 方法 |
| --- | --- | --- |
| 正常完成 | `basicAck` | `TaskProductor.ackTask(deliveryTag, channel, msg)` |
| 需重试 | `basicNack(requeue=true)` | `TaskProductor.nAckTask(deliveryTag, channel, msg)` |
| 拿不到实例锁 | `nack` + 删 ZK 节点 | `nAckTask(instId, deliveryTag, channel, msg)` |
| 完成且持锁 | `ack` + 删 ZK 节点 | `ackTask(instId, deliveryTag, channel, msg)` |
| 重试超限 | 强制 `ack` | 直接 ack，防止毒消息 |

---

## §10 分布式锁（ZooKeeper）

| 项 | 值 |
| --- | --- |
| 路径 | `Inst.ZK_LOCK_PREFIX + instId` = `/INST_LOCK_{instId}` |
| 模式 | `CreateMode.EPHEMERAL` |
| 客户端 | Apache Curator `CuratorFramework` |
| 释放 | `curatorFramework.delete().forPath(...)` |

**加锁时机**：`TaskResultConsumer.listenInstTaskResult`（L82-83）——处理结果消息前先抢锁。
**释放时机**：`TaskProductor.ackTask(instId,...)` / `nAckTask(instId,...)`（L53-69）。

语义：**同一实例的结果消息严格串行处理**，防止「同一实例多个节点结果并发推进」导致状态集合竞争。会话断开时临时节点自动删除，天然避免死锁。

---

## §11 应用配置（`application.yml`）

| 配置项 | 用途 |
| --- | --- |
| `spring.data.mongodb.uri` | MongoDB 连接 |
| `spring.rabbitmq.*` | RabbitMQ 连接（host/port/username/password）|
| `spring.cloud.stream.bindings.*` | 各通道绑定的交换器/队列/分组 |
| `task.run_queue_name` | 任务执行队列名（`TaskProductor` 注入）|
| `task.result_queue_name` | 任务结果队列名（`TaskProductor` 注入）|
| `zookeeper.connect-string` | ZooKeeper 地址（Curator）|
| `server.port` | Web 端口 |

> `TaskProductor` L24-27 通过 `@Value("${task.result_queue_name}")` / `@Value("${task.run_queue_name}")` 注入队列名。

---

## 📚 相关文档
- 鈿欙笍 [鍩疯寮曟搸鑸囪搴︽祦绋媇(03-鍩疯寮曟搸鑸囪搴︽祦绋?md) 鈥?閫欎簺鏁告摎绲愭濡備綍琚畝瀵玚r
- 馃敆 [DSL瑕忕瘎](02-DSL规范.md) 鈥?Inst 涓繚瀛樼殑 DSL 绲愭
- 鈿欙笍 [閰嶇疆瑾槑](12-配置说明.md) 鈥?闆嗗悎銆侀€氶亾銆乑K 鐨勯€ｆ帴閰嶇疆
- 馃摑 [REST鎺ュ彛瑾槑](05-REST接口说明.md) 鈥?瀵︿緥鐙€鎱嬪浣曡鏌ヨ
- 馃殌 [蹇€熼枊濮媇(11-蹇€熼枊濮?md) 鈥?涓枔浠跺噯鍌檂r
