# 服务编排平台 · 项目总览

## 📋 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 项目总览 |
| 文档版本 | v1.0 |
| 最后更新 | 2026-09-14 |
| 状态 | 现行 |
| 适用读者 | 全体 |

---

## 目录

- [§1 项目定位](#§1-项目定位)
- [§2 模块结构](#§2-模块结构)
- [§3 技术栈](#§3-技术栈)
- [§4 核心概念速览](#§4-核心概念速览)
- [§5 架构总览](#§5-架构总览)
- [§6 端到端时序](#§6-端到端时序)
- [§7 文档索引](#§7-文档索引)
- [§8 实现状态与待办](#§8-实现状态与待办)

---

## §1 项目定位

本项目是一个 **基于 DSL 的轻量级服务编排（Service Arrangement）引擎**。它把多个分散的服务/接口（HTTP、Dubbo、WebService、Shell/Jar/Python 脚本、内部算子）通过一份可视化编排的 JSON（DSL）串成一条可执行流程，并负责：

- 流程的**解析**（DSL → 实例图）
- 流程的**调度与执行**（消息驱动、节点级任务、分布式锁）
- 流程的**状态与日志记录**（实例、任务、日志三级）
- **流程出错时的补偿与回滚**（补偿强组合）
- 流程的**循环**（doWhile / whileDo）与**条件分支**（决策线）

> 定位类比：轻量版「阿里云 OSS 工作流 / 腾讯云 TDSQL 编排 / BPMN 引擎」，更强调「服务调用」而非「人工审批」。

## §2 模块结构

```
service_arrange/                      # 仓库根（父 pom，聚合模块）
├── pom.xml
├── README.md                         # 快速上手（详见 docs/）
├── docs/                             # 本套文档（入口：文档说明.md）
│
├── frontend/                         # 可视化编排控制台（纯静态，零依赖）
│   ├── index.html                    # 4 视图 + 抽屉 + 模态
│   ├── css/style.css
│   └── js/                           # 8 个模块：schema/nodes/validator/store/api/designer/views/app
│
├── servicearrange/                   # 后端（Maven 多模块）
│   ├── pom.xml                       # 后端父 pom
│   ├── servicearrangeImpl/           # 核心引擎（无 Web 层）
│   │   └── src/main/java/com/hrh/servicearrange/
│   │       ├── dsl/                  # DSL 数据结构（Cell/DSL/Group/各节点 data）
│   │       ├── entity/               # 持久化实体（Inst/Task/InstLog/RegisterInfoEntity）
│   │       ├── parser/               # DSL 解析器 DslParser + 注解（CellType/TaskType/LoopType）
│   │       ├── convert/              # Cell → Task 转换器（TaskInterface / StartTask / EndTask）
│   │       ├── executor/             # 执行器（Execute 接口 + impl 各算子实现）
│   │       ├── mq/                   # 消息驱动（生产者/消费者/通道定义）
│   │       ├── dao/                  # MongoDB 数据访问
│   │       ├── service/              # 业务服务（任务服务、算子注册服务）
│   │       ├── config/               # 配置类（MongoDBConfig / ZookeeperConfig）
│   │       ├── utils/                # 工具类（JsonSchemaUtil / SpringBeanUtils）
│   │       └── vo/                   # 出入参对象（InstRunParamsVo/InstRunResponseVo/Task4MQ）
│   └── main/                         # Web 启动模块
│       └── src/main/
│           ├── java/com/hrh/servicearrange/
│           │   ├── ServiceArrangeApplication.java
│           │   └── controller/       # InstController / RegisterInfoController
│           └── resources/
│               ├── application.yml   # 全部配置（含 Apollo/Eureka/Stream/Mongo/ZK）
│               ├── hrh_http.json     # 示例 DSL（HTTP 流程）
│               ├── hrh_dslDemo1.json # 示例 DSL（带补偿的流程）
│               ├── 决策线节点.json    # 示例 DSL 片段（决策线/映射节点）
│               ├── nodeInfo.json     # 示例：数据映射节点片段
│               ├── 流程.txt           # 需求：完整流程说明
│               └── problem.txt       # 需求：事务一致性要解决的问题
│
└── 服务编排关键技术.docx              # 原始需求/技术文档
```

> 后端**仅 54 个 Java 文件**，模块粒度很轻：`servicearrangeImpl` 承载全部引擎逻辑，`main` 只做 Web 接入。详细包级职责见 [系统架构](15-系统架构.md) §3。

## §3 技术栈

| 层次 | 技术 |
| --- | --- |
| 语言/构建 | Java 8+、Maven 多模块 |
| Web | Spring Boot、Spring MVC、`spring-boot-maven-plugin` |
| 消息 | Spring Cloud Stream + RabbitMQ（4 个通道，手工 ACK）|
| 存储 | Spring Data MongoDB（`SERVEA_INST` / `SERVEA_TASK` / `SERVEA_INST_LOG` / `REGISTER_INFO`）|
| 分布式协调 | Apache Curator + ZooKeeper（实例级分布式锁 `/INST_LOCK_{instId}`）|
| 表达式 | Spring SpEL（决策/循环条件求值）+ 自定义取值表达式 |
| 工具 | Hutool（JSON/File/StrUtil/HTTP/SpringUtil）、Lombok |
| 配置中心 | Apollo（已声明，`eagerLoad=false`，未实际启用）|
| 服务注册 | Eureka（已声明，编排逻辑未使用服务发现）|
| 前端 | 原生 HTML5 + CSS3 + ES5 JavaScript（零依赖、零构建）|

> 版本与用途的完整表格见 [系统架构](15-系统架构.md) §2；连接串与端口见 [配置说明](12-配置说明.md)。

## §4 核心概念速览

| 概念 | 说明 |
| --- | --- |
| **DSL** | 一份 JSON，描述流程的画布元素（节点 + 连线）与强组合 |
| **Cell** | 节点或连线的统称，节点与连线同构 |
| **Inst（实例）** | 一次流程运行的上下文：解析后的图、全局参数、各节点状态集合、出参、日志 |
| **Task（任务）** | 一个节点在某个循环次数下的一次执行记录 |
| **InstLog（日志）** | 实例/节点级日志 |
| **决策线** | 条件线，条件成立才流转到目标节点 |
| **循环线** | 圈定一段区间并定义循环退出条件 |
| **补偿线 + 强组合** | 节点失败后逆序触发补偿节点，实现局部事务回滚 |
| **算子** | 可被编排的最小执行单元（HTTP/Dubbo/WebService/脚本/内部算子）|
| **算子注册** | 通过 `RegisterInfoEntity` 登记外部 API 元信息，供编排时复用 |

## §5 架构总览

```
                    ┌──────────────────────────────┐
   浏览器            │   前端控制台 frontend/        │
   (设计/运行/监控)   │  设计器 · 实例 · 算子 · 参数    │
                    └───────────────┬──────────────┘
                                    │ REST
   ┌────────────────────────────────▼──────────────────────────────────┐
   │ main 模块（Web 层）                                                │
   │  InstController  POST /inst/run/or/getResult/{planId}             │
   │  RegisterInfoController  /registerOperator/save|delete|findById   │
   └────────────────────────────────┬──────────────────────────────────┘
                                    │
   ┌────────────────────────────────▼──────────────────────────────────┐
   │ servicearrangeImpl 核心引擎                                       │
   │                                                                   │
   │  DslParser  ──解析──▶  Inst（nodeMap/入出度/父子关系/循环区间）    │
   │      │                                                            │
   │      ▼                                                            │
   │  TaskInterface(convert) ──▶ Task ──▶ TaskProductor ──▶ MQ         │
   │                                            │                      │
   │                          TaskRunConsumer ◀─┘  output_channel      │
   │                                   │                               │
   │                          Execute.runProcess(task)                 │
   │                          （按 cellType 路由到算子实现）             │
   │                                   │                               │
   │                          TaskProductor.sendTaskResult             │
   │                                   │                               │
   │                          TaskResultConsumer  ◀─ task_result_...   │
   │                          （决策/循环/补偿/下一节点编排）            │
   └────────────────────────────────┬──────────────────────────────────┘
                                    │
        ┌───────────────────────────┼───────────────────────────┐
        ▼                           ▼                           ▼
   RabbitMQ                    MongoDB                     ZooKeeper
   (任务/结果队列)          (Inst/Task/Log/Register)      (实例分布式锁)
```

## §6 一次运行的端到端时序

1. 前端（或调用方）`POST /inst/run/or/getResult/{planId}` 提交表单参数。
2. `InstController` 读取 DSL、解析开始节点 `inputsJsonSchema`、把表单值按类型还原为 JSON，并采集全部请求头存入 `headerParams`。
3. `DslParser.parser(dsl)` 生成 `Inst`（图结构 + 全局参数 + 循环区间），补全 `planId/optType/sync/入参/请求头`，落库（先 `WAITE` 再 `RUNNING`）。
4. `servea_optType=run` → 取 `roots` 中的开始节点，`StartTask.convert` 生成任务，`TaskProductor` 触发执行。
5. `TaskRunConsumer` 收到任务 → 按 `task.type`（即 cellType）取到 `Execute` Bean → `runProcess` 执行 → 回写状态与出参。
6. `TaskResultConsumer` 收到结果：
   - 加实例锁（ZK 临时节点 `/INST_LOCK_{instId}`）；
   - 更新实例的 `success / fail / skip` 节点集合与实例状态；
   - 处理**补偿**（失败且有补偿线）、**循环**（doWhile / whileDo）、**决策**（决策线是否通过）；
   - 计算「所有父节点均已成功或跳过」的下一批节点，转 Task 并投递；
   - 若当前是结束节点，则落实例出参并置 `SUCCESS`。
7. `servea_optType=get_result` 时再次调用接口，读取实例状态与出参返回。

> ⚠️ 当前实现有两处偏离上述设计：**DSL 硬编码读取 `hrh_http.json`**（`planId` 不生效）；**`TaskProductor` 为本地直调而非真正投递 MQ**。详见 [项目现状](10-项目现状.md)。

## §7 文档索引

| 文档 | 内容 |
| --- | --- |
| 📘 [文档说明](文档说明.md) | 分类索引与体例基准（**建议从这里开始**）|
| 🚀 [快速开始](11-快速开始.md) | 本地启动与验证 |
| 🗺️ [项目现状](10-项目现状.md) | 哪些能跑、哪些不能跑、缺陷与路线 |
| ✅ [功能清单](09-功能清单.md) | 15 个功能域 204 项功能的实现状态 |
| 🏗️ [系统架构](15-系统架构.md) | 分层架构、模块职责、非功能设计 |
| 🔗 [DSL规范](02-DSL规范.md) | DSL 完整字段规范与校验规则 |
| ⚙️ [执行引擎与调度流程](03-执行引擎与调度流程.md) | 解析、转换、执行、编排四大阶段 |
| 🗄️ [数据模型与消息模型](04-数据模型与消息模型.md) | 实体字段、Mongo 集合、MQ 通道、ZK 锁 |
| 📝 [REST接口说明](05-REST接口说明.md) | 全部 HTTP 接口 |
| 💻 [前端控制台说明](06-前端控制台说明.md) | 控制台操作手册与接口契约 |
| 🧩 [算子扩展指南](07-算子扩展指南.md) | 新增算子的完整步骤 |
| 📐 [编码规范](16-编码规范.md) | 命名约定与贡献流程 |
| ⚙️ [配置说明](12-配置说明.md) | `application.yml` 逐项说明 |
| 🐳 [部署指南](13-部署指南.md) | 部署流程与容器化 |
| 🧪 [测试指南](14-测试指南.md) | 验证清单与缺陷复现 |
| 📊 [架构图与实现状态](08-架构图与实现状态.md) | 架构图集与状态对照表 |

## §8 实现状态与待办

以下是阅读源码时发现的**未完成点**，建议在后续迭代中补齐（本项目按要求**不实现后端**，此处仅记录）：

| # | 位置 | 现状 |
| --- | --- | --- |
| 1 | `InstController#run` 第 80 行 | DSL 被硬编码为读取 classpath 下的 `hrh_http.json`，未使用真实 `planId` 关联的模型 |
| 2 | `InstController` | 未做 `multipart/form-data` 之外的内容类型校验（相关异常被注释掉，第 77 行）|
| 3 | `InstController` 第 210-229 行 | 文件下载分支只有 TODO 注释，未实现；且 `fileName` 可能为 `null` 导致 NPE |
| 4 | `InstController` 第 139-146 行 | 上传文件仅 `System.out.println`，未落盘 |
| 5 | `TaskResultConsumer` 第 381-385 行 | `dicisionEdges` 变量被赋值为 `null` 且未接收 `filter` 结果，导致决策线判断实际永不生效（**P0**）|
| 6 | `TaskResultConsumer` 第 334 行 | whileDo 分支里用了 `outLoopEdges.get(0)`，`outLoopEdges` 可能为 `null`（**P0 NPE 风险**）|
| 7 | `TaskResultConsumer` 第 389 行 | `pSkipNum == skipNodes.size()` 语义应为 `pids.size()` |
| 8 | `convert` 包 | 24 种类型中只有 2 个 `TaskInterface` 实现（`node_start_task` / `node_end_task`），缺 22 个（**P0**）|
| 9 | `executor/impl` 包 | 24 种类型中仅 4 个执行器实现，Dubbo/WebService/脚本/补偿类算子全缺（**P1**）|
| 10 | `TaskProductor` 第 42 / 49 行 | `sendTaskRun` / `sendTaskResult` 为本地直调，未真正投递 MQ |
| 11 | `TaskType.java` 第 25-47 行 | 除 start/end 外的 TaskType 常量被整体注释，实际靠 `cellType + "_task"` 约定注册 |
| 12 | `DslParser` 第 261 / 278 行 | 递归出口用 `static` 计数器，非线程安全 |
| 13 | `DslParser` 第 33 / 139 / 152 行 | `Optional.get()` 未判空 |
| 14 | 无模型（Plan）管理 | 缺少 `Plan` 实体与 CRUD 接口，`planId` 目前无实际存储对象 |
| 15 | 命名拼写 | `Inst.starDate` / `InstLog.LEVEL_ERRO` / `RegisterInfoEntity.sheelFtpPath` |

> 完整缺陷清单（含行号与修复建议）见 [项目现状](10-项目现状.md) §3；逐项功能状态见 [功能清单](09-功能清单.md)。

---

## 📚 相关文档
- 🗺️ [项目现状](10-项目现状.md) — 读完本篇后建议立即了解可用性边界
- 🏗️ [系统架构](15-系统架构.md) — 分层架构与模块职责的展开
- 🔗 [DSL规范](02-DSL规范.md) — 核心契约
- 🚀 [快速开始](11-快速开始.md) — 立刻动手跑起来
- 📊 [架构图与实现状态](08-架构图与实现状态.md) — 架构图集
- 📘 [文档说明](文档说明.md) — 全部文档索引
